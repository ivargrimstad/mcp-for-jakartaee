/**
 * MCP (Model Context Protocol) Server implementation for Jakarta EE.
 * 
 * This package provides a complete implementation of the MCP protocol,
 * enabling AI models to interact with Jakarta EE applications through
 * a standardized JSON-RPC 2.0 based protocol.
 * 
 * Key components:
 * - endpoint: JAX-RS REST endpoints for MCP protocol
 * - protocol: Core MCP protocol handler and state management
 * - model: Data models for JSON-RPC and MCP protocol
 * - registry: Tool, Resource, and Prompt registries
 * - tool: Tool interface and implementations
 * - resource: Resource interface and implementations
 * - prompt: Prompt interface and implementations
 */
package dukes.mcp;
